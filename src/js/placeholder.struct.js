export default function placeholder(styles) {
    const placeholder = {
        tag: "div",
        attributes: { class: "grid-item--placeholder" },
        styles: styles,
    };
    return placeholder;
}
